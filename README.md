# interactive_GHG_plots

Visualising ACRG data. Replace the data folder with a provided zip.

Use the same env as GATES, plus import `ipymlp` (e.g. using `conda install conda-forge::ipympl`) for interactive widgets.

TODO: Write docstrings, add instructions to notebook.